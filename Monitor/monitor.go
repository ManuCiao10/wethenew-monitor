package monitor

import (
	"main/data"
)


func Check(class data.Info) {
	// loop in the struct and check if there is a new product
	// if there is a new product send a webhook
	// discord.Webhook(result)
}